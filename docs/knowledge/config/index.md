# Config

* [bundled-iana-data](bundled-iana-data.md) - The IANA root-hints and TLD-list snapshots compiled
  into the binary, how each is parsed at runtime, and how CI tells real upstream drift from IANA's
  per-publish version/date headers.
